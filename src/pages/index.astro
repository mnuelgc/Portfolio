---
import HomePage from "../components/pages/HomePage.astro";
import type { Locale } from "../i18n/ui";

const locale = (Astro.currentLocale ?? "en") as Locale;
---

<HomePage locale={locale} pathname={Astro.url.pathname} />
